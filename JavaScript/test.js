const assert = require('assert');
const solution = require('./smallestPossibleSum/solution');

describe('Example test', () => {
  it('does the thing', () => {
    assert.equal(solution([6, 9, 21]), 9);
  });
});

